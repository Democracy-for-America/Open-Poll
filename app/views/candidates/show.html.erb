  <style>
    body { padding-top: 150px; }
  </style>
  <div class="container">
    <div class="sixteen columns">
      <h3 style="text-transform: uppercase; letter-spacing: -1px; font-weight: bold; color: #333;"><%= @candidate.name %></h3>
      <div><%= simple_format @candidate.description %></div>
    </div>
  </div>