# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  admin_user: admin
  admin_password: password
  secret_key_base: ac3026a500c7586859bcad3152d80357cb51472f4d2a6de68f4b8e6c02f1f0c48f237c62be3174aa67b5e18ee40875725611259eb16e176e4614f89305f07f81

test:
  admin_user: admin
  admin_password: password
  secret_key_base: 1a3c96303e8e3126d89884bd7e050722e22674f5c6d216c20c5b380538521f7b092ce161369b6622bae848664848780d99020620f3b8edd39069311b7372156c

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  admin_user: <%= ENV["ADMIN_USER"] %>
  admin_password: <%= ENV["ADMIN_PASSWORD"] %>
  s3_bucket: <%= ENV["S3_BUCKET"] %>
  s3_access_key_id: <%= ENV["S3_ACCESS_KEY_ID"] %>
  s3_secret_access_key: <%= ENV["S3_SECRET_ACCESS_KEY"] %>